export const dictionary: Record<string, Record<string, string[]>> = {
  "*": {
    Usage: ["Usage"],
    "Initialize the ROADMAP.md file if not exists": [
      "Initialize the ROADMAP.md file if not exists",
    ],
    Commands: ["Commands"],
  },
};
